binaries:
  - placeholder
directories:
  - mode: "0775"
    paths:
      - /bitnami/placeholder
      - /opt/bitnami/placeholder/conf
files:
  - paths:
      - /opt/bitnami/placeholder/logs.log
root_dir: /opt/bitnami
version:
  bin_name: placeholder
  flag: --version
