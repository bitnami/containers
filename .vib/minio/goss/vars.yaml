binaries:
  - minio
  - mc
  - wait-for-port
directories:
  - mode: "0775"
    paths:
      - /bitnami/minio/data
      - /certs
      - /opt/bitnami/minio/log
      - /opt/bitnami/minio/tmp
      - /opt/bitnami/minio/secrets
root_dir: /opt/bitnami
