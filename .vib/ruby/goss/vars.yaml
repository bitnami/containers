binaries:
  - bundle
  - bundler
  - gem
  - rake
  - ruby
modules:
  - bundler
  - openssl
  - readline
directories:
  - mode: "0755"
    paths:
      - /opt/bitnami/ruby
root_dir: /opt/bitnami
version:
  bin_name: ruby
  flag: -v
