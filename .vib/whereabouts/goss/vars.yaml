binaries:
  - whereabouts
  - ip-control-loop
  - install-cni.sh
files:
  - mode: "0755"
    paths:
      - /opt/bitnami/whereabouts/bin/install-cni.sh
root_dir: /opt/bitnami
version:
  bin_name: whereabouts
  flag: --version
