files:
  - mode: "0644"
    paths:
      - /opt/bitnami/minio-object-browser/.spdx-minio-object-browser.spdx
  - mode: "0755"
    paths:
      - /opt/bitnami/minio-object-browser/bin/console
version:
  bin_name: console
  flag: --version
