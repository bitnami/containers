binaries:
  - node
  - npm
directories:
  - paths:
      - /opt/bitnami/supabase/studio/public
      - /opt/bitnami/supabase/studio/.next/static
files:
  - paths:
      - /opt/bitnami/supabase/package-lock.json
root_dir: /opt/bitnami
sed_in_place:
  exclude_paths:
    - /opt/bitnami/supabase/node_modules/
