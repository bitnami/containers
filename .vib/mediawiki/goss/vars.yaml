binaries:
  - apache
  - libphp
  - mediawiki
  - mysql-client
  - php
  - render-template
directories:
  - paths:
      - /opt/bitnami/mediawiki/vendor/pear/mail
      - /opt/bitnami/mediawiki/vendor/pear/net_smtp
      # Validate that a supported web server is configured
      - /opt/bitnami/scripts/apache-env.sh
      - /opt/bitnami/scripts/libapache.sh
root_dir: /opt/bitnami
