binaries:
  - memcached_exporter
files:
  - mode: "0644"
    paths:
      - /opt/bitnami/memcached-exporter/.spdx-memcached-exporter.spdx
  - mode: "0755"
    paths:
      - /opt/bitnami/memcached-exporter/bin/memcached_exporter
root_dir: /opt/bitnami
version:
  bin_name: memcached_exporter
  flag: --version
