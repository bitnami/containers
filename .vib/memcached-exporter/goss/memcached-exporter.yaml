file:
  # Symlink test template
  /opt/bitnami/memcached-exporter/folder:
    exists: true
    filetype: symlink
    linked-to: /folder
  # Removed element test template
  /opt/bitnami/memcached-exporter/element:
    exists: false
  # File contents test template
  /opt/bitnami/memcached-exporter/config.conf:
    exists: true
    filetype: file
    contains:
      - /memcached-exporter1/
      - memcached-exporter
