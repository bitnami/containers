binaries:
  - memcached
directories:
  - mode: "0775"
    paths:
      - /opt/bitnami/memcached/conf
      - /opt/bitnami/memcached/conf/sasl2
memcached:
  port: 11211
  user: 1001
root_dir: /opt/bitnami
version:
  bin_name: memcached
  flag: --version
