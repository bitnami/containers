files:
  - mode: "0644"
    paths:
      - /opt/bitnami/argo-workflows/.spdx-argo-workflows.spdx
  - mode: "0755"
    paths:
      - /opt/bitnami/argo-workflows/bin/argo
version:
  bin_name: argo
  flag: version
