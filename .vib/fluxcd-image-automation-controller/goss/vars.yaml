binaries:
  - fluxcd-image-automation-controller
directories:
  - mode: "0775"
    paths:
      - /bitnami/fluxcd-image-automation-controller
      - /opt/bitnami/fluxcd-image-automation-controller/conf
files:
  - paths:
      - /opt/bitnami/fluxcd-image-automation-controller/logs.log
root_dir: /opt/bitnami
version:
  bin_name: fluxcd-image-automation-controller
  flag: --version
