binaries:
  - influx
  - influxd
  - wait-for-port
directories:
  - mode: "0775"
    paths:
      - /opt/bitnami/influxdb/etc
      - /bitnami/influxdb
      - /docker-entrypoint-initdb.d
files:
  - paths:
      - /.influx_history
root_dir: /opt/bitnami
version:
  bin_name: influxd
  flag: version
