binaries:
  - apachectl
  - mariadb
  - php
  - render-template
files:
  - paths:
      - /opt/bitnami/joomla/.htaccess
directories:
  - mode: "0775"
    paths:
      - /bitnami/joomla
      - /opt/bitnami/joomla
      - /opt/bitnami/joomla/logs
      - /opt/bitnami/joomla/tmp
root_dir: /opt/bitnami
