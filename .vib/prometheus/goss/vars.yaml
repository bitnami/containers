binaries:
  - prometheus
  - promtool
directories:
  - mode: "0775"
    paths:
      - /opt/bitnami/prometheus/data
files:
  - paths:
      - /opt/bitnami/prometheus/conf/prometheus.yml
root_dir: /opt/bitnami
version:
  bin_name: prometheus
  flag: --version
