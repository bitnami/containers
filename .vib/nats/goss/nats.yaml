command:
  check-nats-version:
    exec:
    - /nats-server
    - --version
    exit-status: 0
    stdout:
    - "{{ .Env.APP_VERSION }}"
  check-nats-server:
    exec:
    - /nats-server
    - --help
    exit-status: 0
    stdout:
    - "Usage: nats-server [options]"
