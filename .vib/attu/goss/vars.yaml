binaries:
  - node
  - yarn
  - npm
root_dir: /opt/bitnami
directories:
  - paths:
      # Paths to the client and server directories
      - /opt/bitnami/pymilvus/dist
      - /opt/bitnami/pymilvus/build
version:
  bin_name: cd /opt/bitnami/pymilvus; npm
  flag: list /opt/bitnami/pymilvus || true
