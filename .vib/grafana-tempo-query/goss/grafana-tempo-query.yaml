file:
  ## tempo-query does not return the version so we check the license
  /opt/bitnami/grafana-tempo-query/licenses/agpl-source-links.txt:
    exists: true
    contains:
      - {{ .Env.APP_VERSION }}
