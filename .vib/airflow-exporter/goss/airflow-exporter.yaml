command:
  check-airflow-exporter-run:
    exec: AIRFLOW_PROMETHEUS_DATABASE_NAME=mydatabase timeout 2 airflow-prometheus-exporter
    timeout: 5000
    exit-status: 124
    stderr:
      - Starting airflow-exporter
      - mydatabase
