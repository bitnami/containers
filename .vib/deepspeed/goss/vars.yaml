binaries:
   - python
   - deepspeed
root_dir: /opt/bitnami
directories:
  - paths:
      # Paths to the client and server directories
      - /opt/bitnami/python
version:
  bin_name: python
  flag: "-c 'import deepspeed; print(deepspeed.__version__)'"
