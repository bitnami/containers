binaries:
  - orderer
directories:
  - mode: "0755"
    paths:
      - /opt/bitnami/hyperledger-fabric-orderer/etc
  - mode: "0775"
    paths:
      - /opt/bitnami/hyperledger-fabric-orderer/var
  - paths:
      - /opt/bitnami/hyperledger-fabric-orderer
root_dir: /opt/bitnami
version:
  bin_name: orderer
  flag: version
