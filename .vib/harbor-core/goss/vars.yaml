binaries:
  - harbor_core
  - ini-file
  - wait-for-port
directories:
  - paths:
      - /etc/core/certificates
      - /etc/core/ca_download
      - /etc/core/token
      - /opt/bitnami/harbor-core/views
      - /opt/bitnami/harbor-core/migrations
      - /opt/bitnami/harbor-core/icons
files:
  - paths:
      - /opt/bitnami/harbor-core/UIVERSION
root_dir: /opt/bitnami
